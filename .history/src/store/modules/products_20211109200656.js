
import { db } from '../firebase'


// initial state
const state = () => ({
    games: [],
    productsLoaded: null, 
    
})

// getters
const getters = {
    
}

// actions
const actions = {
   
}

// mutations
const mutations = {
  

    
    
  }

  

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}